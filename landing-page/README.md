# JobSyte Landing Page

Standalone Next.js App Router site for the JobSyte public marketing pages.

```bash
npm install
npm run dev
```

The demo request form posts to `app/api/demo-request/route.ts` and expects the Resend-related environment variables from `.env.example`.
